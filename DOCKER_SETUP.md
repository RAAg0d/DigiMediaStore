# 🐳 Инструкция по развертыванию DigiMediaStore с Docker

## 📋 Требования

- Docker Desktop (или Docker Engine + Docker Compose)
- Минимум 4GB свободной оперативной памяти
- Порты 3000, 5432, 8080 должны быть свободны

## 🚀 Быстрый старт

### 1. Клонирование и подготовка

```bash
# Убедитесь, что вы находитесь в корневой директории проекта
cd DigiMediaStore
```

### 2. Запуск всех сервисов

```bash
docker-compose up --build
```

Эта команда:
- Соберет образы для API и Frontend
- Запустит PostgreSQL базу данных
- Запустит ASP.NET Core API
- Запустит React Frontend

### 3. Доступ к приложению

После успешного запуска приложение будет доступно по следующим адресам:

- **Frontend**: http://localhost:3000
- **API**: http://localhost:8080
- **Swagger UI**: http://localhost:8080/swagger
- **PostgreSQL**: localhost:5432

## 🔧 Управление контейнерами

### Остановка сервисов

```bash
docker-compose down
```

### Остановка с удалением volumes (удалит данные БД)

```bash
docker-compose down -v
```

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только API
docker-compose logs -f api

# Только Frontend
docker-compose logs -f frontend

# Только PostgreSQL
docker-compose logs -f postgres
```

### Перезапуск конкретного сервиса

```bash
docker-compose restart api
docker-compose restart frontend
docker-compose restart postgres
```

## 🗄️ Работа с базой данных

### Подключение к PostgreSQL

```bash
docker-compose exec postgres psql -U postgres -d DigiMediaStore
```

### Выполнение миграций

Миграции должны выполняться автоматически при первом запуске. Если нужно выполнить вручную:

```bash
# Войдите в контейнер API
docker-compose exec api bash

# Выполните миграции
dotnet ef database update --project DigiMediaStore.DataAccess
```

## 🔐 Переменные окружения

Вы можете настроить переменные окружения, создав файл `.env` в корне проекта:

```env
POSTGRES_DB=DigiMediaStore
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password
API_PORT=8080
FRONTEND_PORT=3000
```

## 🐛 Решение проблем

### Порт уже занят

Если порт занят, измените порты в `docker-compose.yml`:

```yaml
ports:
  - "8081:8080"  # Вместо 8080:8080
```

### Ошибка подключения к БД

1. Убедитесь, что PostgreSQL контейнер запущен: `docker-compose ps`
2. Проверьте логи: `docker-compose logs postgres`
3. Убедитесь, что в `appsettings.json` используется правильная строка подключения

### Frontend не подключается к API

Проверьте переменную окружения `REACT_APP_API_URL` в `docker-compose.yml`. Она должна указывать на правильный адрес API.

## 📦 Структура Docker

```
DigiMediaStore/
├── DigiMediaStore/
│   └── Dockerfile          # Dockerfile для ASP.NET Core API
├── frontend/
│   ├── Dockerfile          # Dockerfile для React приложения
│   └── nginx.conf          # Конфигурация Nginx
├── docker-compose.yml      # Оркестрация всех сервисов
└── .dockerignore           # Исключения для Docker build
```

## 🌐 Публикация на хостинг

### Варианты хостинга:

1. **Docker Hub + VPS** (DigitalOcean, AWS, Azure)
2. **Heroku** (с Docker support)
3. **Railway.app**
4. **Render.com**
5. **Fly.io**

### Пример для Railway/Render:

1. Подключите репозиторий GitHub
2. Укажите `docker-compose.yml` как конфигурацию
3. Настройте переменные окружения
4. Деплой произойдет автоматически

### Пример для VPS:

```bash
# На сервере
git clone <your-repo>
cd DigiMediaStore
docker-compose up -d --build
```

## ✅ Проверка работы

1. Откройте http://localhost:3000
2. Проверьте статус подключения к API
3. Нажмите "Загрузить контент" или "Загрузить жанры"
4. Откройте Swagger UI по адресу http://localhost:8080/swagger

## 📝 Примечания

- Данные PostgreSQL сохраняются в Docker volume `postgres_data`
- При первом запуске может потребоваться время на сборку образов
- Swagger UI доступен в Production для удобства тестирования









